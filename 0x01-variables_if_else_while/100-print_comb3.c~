#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
for (int i = 48 ; i < 58 ; i++)
{
for (int j = i + 1 ; j < 58 ; j++)
{
putchar(i);
putchar(j);
if (i < 56)
{
putchar(32);
putchar(44);
};
}
}
putchar('\n');
return (0);
}
